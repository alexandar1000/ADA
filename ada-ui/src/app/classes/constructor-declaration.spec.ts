import { ConstructorDeclaration } from './constructor-declaration';

describe('ConstructorDeclaration', () => {
  it('should create an instance', () => {
    expect(new ConstructorDeclaration()).toBeTruthy();
  });
});
