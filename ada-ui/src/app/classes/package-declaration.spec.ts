import { PackageDeclaration } from './package-declaration';

describe('PackageDeclaration', () => {
  it('should create an instance', () => {
    expect(new PackageDeclaration()).toBeTruthy();
  });
});
