import { AttributeDeclaration } from './attribute-declaration';

describe('AttributeDeclaration', () => {
  it('should create an instance', () => {
    expect(new AttributeDeclaration()).toBeTruthy();
  });
});
