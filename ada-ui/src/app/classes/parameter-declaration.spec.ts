import { ParameterDeclaration } from './parameter-declaration';

describe('ParameterDeclaration', () => {
  it('should create an instance', () => {
    expect(new ParameterDeclaration()).toBeTruthy();
  });
});
