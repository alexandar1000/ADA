import { AttributeInvocation } from './attribute-invocation';

describe('AttributeInvocation', () => {
  it('should create an instance', () => {
    expect(new AttributeInvocation()).toBeTruthy();
  });
});
