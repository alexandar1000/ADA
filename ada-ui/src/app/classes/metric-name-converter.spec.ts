import { MetricNameConverter } from './metric-name-converter';

describe('MetricNameConverter', () => {
  it('should create an instance', () => {
    expect(new MetricNameConverter()).toBeTruthy();
  });
});
