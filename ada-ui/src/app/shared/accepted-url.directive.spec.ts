import { AcceptedUrlDirective } from './accepted-url.directive';

describe('AcceptedUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new AcceptedUrlDirective();
    expect(directive).toBeTruthy();
  });
});
