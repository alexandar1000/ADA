package package1;

public class Cat {

    public void catchMouse() {
        int eatAll = Fish.WEIGHT;
        Fish fish = new Fish();
        int fishSpeed = fish.SPEED;
        int lives = 9;
        if (Dog.LENGTH > 6) {
            Fish.WEIGHT++;
        }
    }

    public void eat(Animal a) {}
}
