package com.ucl.ADA.metric_calculator.metrics_structure;

import org.junit.jupiter.api.Test;

class ProjectMetricsContainerTest {
    @Test
    void testChangeAfterOriginalObjectChanged() {
//        ArrayList<String> originArray = new ArrayList<>(Arrays.asList("Aaa", "Bbb", "Ccc", "Ddd", "Eee", "Fff"));
//        ProjectMetricsContainer projectMetricsContainer = new ProjectMetricsContainer(originArray);
//        assertThat(projectMetricsContainer.getClassNames()).containsExactlyElementsOf(originArray);
//
//         Clone the origin and remove an element from the origin array
//        ArrayList<String> clonedArray = new ArrayList<>(Arrays.asList("Aaa", "Bbb", "Ccc", "Ddd", "Eee", "Fff"));
//
//        originArray.remove(5);
//
//        assertThat(projectMetricsContainer.getClassNames()).containsExactlyElementsOf(clonedArray);
//        assertThat(projectMetricsContainer.getClassNames()).hasSize(6);
    }
}